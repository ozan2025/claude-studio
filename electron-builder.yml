appId: com.claude-studio.app
productName: Claude Studio
directories:
  buildResources: resources
  output: dist
files:
  - out/**/*
  - "!out/**/*.map"
mac:
  category: public.app-category.developer-tools
  target:
    - target: dmg
      arch:
        - arm64
        - x64
  artifactName: ${name}-${version}-${arch}.${ext}
